package com.dataproject.btrip.mapper;

import org.springframework.stereotype.Component;

import com.dataproject.btrip.dto.TourDTO;
import com.dataproject.btrip.entity.Tour;

@Component
public class TourMapper {
	
	/*
	 * public TourDTO toDTO(Tour tour) {
	 * 
	 * int id = tour.getId(); String title = tour.getTitle(); String link_url =
	 * tour.getLink_url(); String img_url = tour.getImg_url(); int views =
	 * tour.getViews(); int comments = tour.getComments(); int likes =
	 * tour.getLikes(); String category = tour.getCategory(); int category_num =
	 * tour.getCategory_num();
	 * 
	 * return TourDTO(tour); }
	 */

}
